<?php
    // require_once '../../db_connect.php';

    // session_start();

    // $_SESSION['selecting_result'] = $db->query($_SESSION['bostont']);
    // $result = $_SESSION['selecting_result'];
    
    // if ($result) {
    // echo $db->affected_rows." added to cart.";
    // } else {
    // echo "An error has occurred. The item was not added.";
    // }
    // $db->close();

?>